---
"waratah": minor
---

Replace the authored `cron` channel with `defineSchedule` under `agent/schedules/`. Session triggers now use `schedule` instead of `cron`; a schedule is no longer a channel.
