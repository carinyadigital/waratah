# The ready queue. Capped as policy, filled only by humans.
#
# Publishing rate is pinned to human review capacity, permanently. The cap is
# the number of briefs one reviewer can honestly carry at once; promotion is
# via `pnpm tsx agents/content/ops/queue/promote.ts --slug <slug> --by <your name>`.
cap: 3
ready:
  - measuring-soil-carbon-baseline
  - slow-roasted-highland-beef
