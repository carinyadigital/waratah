{
  "name": "@carinyaparc/brand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The standards layer. Every agent reads it; no agent writes it. (content/design.md §1)",
  "scripts": {
    "build": "tsx build.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "yaml": "^2.6.0"
  }
}
