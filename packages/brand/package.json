{
  "name": "@carinyaparc/brand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The standards layer. Every agent reads it; no agent writes it.",
  "scripts": {
    "build": "tsx build.ts"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "yaml": "^2.9.0"
  }
}
