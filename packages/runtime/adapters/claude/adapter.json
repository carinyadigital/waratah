{
  "id": "claude",
  "description": "Anthropic Claude — managed scheduled/chat agents and interactive subagents.",
  "modes": ["managed", "subagent"],
  "capabilities": {
    "spendCap": "enforced",
    "schedule": true,
    "secretsIsolation": true
  },
  "options": {
    "type": "object",
    "properties": {
      "mode": { "enum": ["managed", "subagent"] },
      "schedule": { "type": "string" },
      "timezone": { "type": "string" }
    },
    "required": ["mode"]
  }
}
