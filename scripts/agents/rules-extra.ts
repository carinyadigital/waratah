/**
 * R9–R12 land here as their epics deliver them (CNT05, CNT11).
 * Empty on arrival: R1–R8 are the register's launch surface.
 */
import type { Rule } from './rules';

export const extraRules: Rule[] = [];
